 * everywhere where there is a g_assert, should be another check later, so
   that I can compile with G_DISABLE_ASSERT. And if I somehow force a crash
   in some cases.
 * Add the no_inherit flag to _all_ options.
 * rename experience_group_cleanup to something more usefull, since it
   doesn't do any cleanup!

arch-tag: 45211614-40fb-4fb6-adaa-d0f8235c2841
